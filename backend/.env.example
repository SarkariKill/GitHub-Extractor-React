# Azure Blob Storage — fill in your values
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=shipper-labels
AZURE_STORAGE_INPUT_FOLDER=input

# CORS — comma-separated list of allowed origins (use * for development only)
CORS_ORIGINS=*

# File upload limits
MAX_FILE_SIZE_MB=50
